{
  "name": "dinamyt",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "description": "Monorepo del ecosistema DINAMYT (identidad, campeonatos y academia).",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3"
  }
}
